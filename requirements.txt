# FastAPI for the web server
fastapi>=0.68.0

# Uvicorn to run the FastAPI server
uvicorn[standard]>=0.15.0

# Requests to make HTTP requests (used by the geocoder)
requests>=2.25.1

# Pydantic for data validation and models
pydantic>=1.8.0

# To load environment variables (if any)
python-dotenv>=0.19.0

# NumPy (pin to 1.x for spaCy compatibility)
numpy>=1.21.0,<2.0.0

# spaCy for advanced natural language processing
spacy>=3.4.0,<4.0.0

# Additional spaCy models and utilities (for transformers-based models)
spacy-transformers>=1.1.0

# Additional dependencies for proper functionality
typing-extensions>=4.0.0

# OpenAI client for LLM integration via OpenRouter
openai>=1.3.0

# Document Processing and OCR Libraries
# =====================================

# OCR engine for reading scanned documents and images
easyocr>=1.7.0

# PDF text extraction (superior to PyPDF2)
pdfplumber>=0.9.0

# Word document processing
python-docx>=0.8.11

# Excel and CSV processing
pandas>=2.0.0
openpyxl>=3.1.0

# Image processing support for OCR
Pillow>=10.0.0

# File type detection (Windows compatible)
python-magic-bin>=0.4.14

# Additional file format support
xlrd>=2.0.0  # For .xls files
python-multipart>=0.0.6  # For file uploads in FastAPI
PyMuPDF>=1.23.0  # For PDF to image conversion (OCR fallback) 