# Working requirements for Render.com deployment
# Python 3.11 compatible - NO problematic packages

# Core FastAPI dependencies
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
pydantic>=1.8.0
python-dotenv>=0.19.0
requests>=2.25.1

# OpenAI client for LLM integration
openai>=1.3.0

# Basic data processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0

# File upload support
python-multipart>=0.0.6

# Basic document processing (minimal set)
pdfplumber>=0.9.0
python-docx>=0.8.11
openpyxl>=3.1.0

# Image processing
Pillow>=10.0.0,<12.0.0

# File type detection
python-magic>=0.4.27

# Excel support
xlrd>=2.0.0

# NO spaCy - causes blis build issues
# NO easyocr - causes build issues  
# NO PyMuPDF - causes build issues
# NO spacy-transformers - causes build issues
